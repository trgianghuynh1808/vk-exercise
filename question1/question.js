// Câu 1: Slightly Superior

// Vợ viết logic cho function "isFirstSuperior" ck đã khai báo sẵn.

// * Input:
// - arr1: là một array có thể toàn số hoặc toàn string
// - arr2: là một array có thể toàn số hoặc toàn string

// * Output:
// - Hàm sẽ trả về boolean. 2 mảng đầu vào sẽ gần giống như nhau. Chỉ khác với nhau một phần tử. So sánh phần từ khác nhau của 2 mảng. Nếu arr1 lớn hơn arr2 trả về true, ngược lại trả về false

// Ví dụ:

// isFirstSuperior([1, 2, 3], [1, 2, 0]) => true
// isFirstSuperior(["a", "d", "c"], ["a", "b", "c"]) => true
// isFirstSuperior([1, 2, 3], [1, 2, 3]) => false
// isFirstSuperior([1, 2, 3], [1, 2, 4]) => false

function isFirstSuperior(arr1, arr2) {
  // Viết logic xử lý tại đây
}

module.exports = isFirstSuperior;
