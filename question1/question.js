// Câu 1: Tính số que diêm để xếp n nhà

// Vợ viết logic cho function "countStick" ck đã khai báo sẵn

// * Input:
// - "n" là số ngôi nhà cần xây. n có thể là:
// + Số nguyên
// + Nếu n <= 0. Output sẽ bằng 0

// * Output:
// - TH1: nếu n là số nguyên > 0. Trả về số que diêm cần để xây nhà giống như ví dụ trong hình. (hình ở file "question.png" vợ mở xem nhóe)
// - TH2: nếu n <= 0. Trả về 0 que.

// Ví dụ:
// countStick(1) => 6
// countStick(2) => 11
// countStick(3) => 16

function countStick(n) {
  // Viết logic xử lý tại đây
}

module.exports = countStick;
