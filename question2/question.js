// Câu 2: Tìm phần tử nhỏ nhất và lớn nhất trong mảng

// Vợ viết logic cho function "findMinMax" ck đã khai báo sẵn.

// * Input:
// - "arr" là mảng chứa các phần tử. Các phần tử trong mảng có thể là:
// + Số hoặc chuỗi (nếu trong mảng có phần tử là chuỗi cần loại bỏ trước khi xử lý tìm min max)
// + Mảng có thể chí có một phần tử là số. Khi này Max = Min

// * Output:
// Trả về một mảng 2 phần tử có cấu trúc [min, max]

// Ví dụ:
// findMinMax([4, 3, 2, 5, 1]) => [1, 5]
// findMinMax([1]) => [1, 1]
// findMinMax([1, 2, 8, 4, '', 6, 'a', 8, 'c']) => [1, 8]

function findMinMax(arr) {
  // Viết logic xử lý tại đây
}

module.exports = findMinMax;
