// Câu 2: Reverse String function

// Vợ viết logic cho function "reverseStr" ck đã khai báo sẵn.

// * Input:
// - K có arg

// * Output:
// - Trả về function mà function đó nhận vào một chuỗi và trả về một chuỗi đảo ngược

// * Ví dụ:
// const fn = reverseStr(); const result = fn("apple") -> result = "elppa"
// const fn = reverseStr(); const result = fn("pear") -> result = "raep"
// const fn = reverseStr(); const result = fn("") -> result = ""

function reverseStr() {
  // Viết logic xử lý tại đây
}

module.exports = reverseStr;
