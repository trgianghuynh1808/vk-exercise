// Câu 3: Tile Teamwork Tactics

// Vợ viết logic cho function "possibleBonus" ck đã khai báo sẵn.
// Tưởng tượng vợ và ck cùng chơi lắc xí ngầu, vợ là người lắc đầu tiên (tile1) sau đó tới ck (tile2) dự đoán xem ck có đủ khả năng đạt tổng điểm như expect. Và trả về boolean

// * Input:
// - tile1: là số điểm vợ lắc được
// - expect: là tổng điểm mong muốn

// * Output:
// - Trả về boolean. True nếu ck có khả năng đạt tổng điểm expect và ngược lại

// * Ví dụ:
// possibleBonus(1, 5) => true
// possibleBonus(6, 12) => true
// possibleBonus(2, 9) => false
// possibleBonus(1, 8) => false

function possibleBonus(tile1, expect) {
  // Viết logic xử lý tại đây
}

module.exports = possibleBonus;
