// Câu 3: Hum nay có phải ngày tình êu hem?

// Vợ viết logic cho function "timeForNoelLove" ck đã khai báo sẵn.

// * Input:
// - "date" là ngày dùng để kiểm tra:
// + "date" có kiểu dữ liệu là Date
// + Nếu không truyền, sẽ mặc định hiểu là ngày hum nay.

// * Output:
// - Trả về boolean
// + Nếu là ngày Noel tình êu => true
// + Nếu hem phải => false

// P/s: trong JS số tháng sẽ theo kiểu index nha vợ. Ví dụ tháng 1 = 0, tháng 12 = 11

// Ví dụ:
// timeForNoelLove(new Date(2013, 11, 24)) => true
// timeForNoelLove(new Date(3000, 11, 24)) => true
// timeForNoelLove(new Date(3000, 0, 1)) => false

function timeForNoelLove(date) {
  // Viết logic xử lý tại đây
}

module.exports = timeForNoelLove;
